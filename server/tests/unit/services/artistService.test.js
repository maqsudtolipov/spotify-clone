describe(() => {
  it("should throw CastError if input id invlaid", () => {
    expect(1).toEqual(1);
  });

  it("should throw 404 if user not found", () => {});

  it("should throw 404 if user is not an artist", () => {});

  it("should query user return their data", () => {});
});
