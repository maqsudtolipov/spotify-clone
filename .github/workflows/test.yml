name: Run Jest Tests

on:
  push:
    branches:
      - "*"
  pull_request:
    branches:
      - "*"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "16"

      - name: Install dependencies
        run: |
          cd server
          npm install

      - name: Set up environment variables
        run: |
          cd server
          touch .env
          echo "DB_URL=${{ secrets.DB_URL }}" >> .env
          echo "DB_PASS=${{ secrets.DB_PASS }}" >> .env
          echo "NODE_ENV=${{ secrets.NODE_ENV }}" >> .env
          cat .env  # Ensure the .env file contains the expected values

      - name: Run tests
        run: |
          cd server
          npm test
