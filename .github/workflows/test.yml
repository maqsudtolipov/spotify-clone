name: Run Jest Tests

on:
  push:
    branches:
      - "*"
  pull_request:
    branches:
      - "*"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "16"

      - name: Install dependencies
        run: |
          cd server
          npm install

      - name: Set up environment variables
        run: |
          touch server/.env  # Create the .env file in the correct directory
          echo "DB_URL=${{ secrets.DB_URL }}" >> server/.env
          echo "DB_PASS=${{ secrets.DB_PASS }}" >> server/.env
          echo "NODE_ENV=${{ secrets.NODE_ENV }}" >> server/.env
          cat server/.env  # Print out the content of .env to the logs

      - name: Run tests
        run: |
          cd server
          node -r dotenv/config node_modules/.bin/jest  # Ensure dotenv is preloaded before running Jest
